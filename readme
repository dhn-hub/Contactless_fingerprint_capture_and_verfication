In this modern era, verifications and biometrics are performed via contactless devices. This project uses a camera that captures a person's fingerprint, checks it against a stored database performs 
the algorithm and grants access if there is a match
